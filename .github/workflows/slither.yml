name: Slither Analysis

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  analyze:
    name: Slither (SAST)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
      - name: Install Soldeer dependencies
        run: forge soldeer install
      - name: Run Slither
        uses: crytic/slither-action@v0.4.0
        id: slither
        with:
          fail-on: none # advisory during the hackathon; triage findings in PRs
          slither-args: --filter-paths "dependencies/" --exclude-dependencies
          sarif: results.sarif
      # Code scanning upload needs GitHub Advanced Security (not on free private
      # repos). Skip cleanly while private; uploads automatically once public.
      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always() && steps.slither.outputs.sarif != '' && github.event.repository.visibility == 'public'
        continue-on-error: true
        with:
          sarif_file: ${{ steps.slither.outputs.sarif }}
